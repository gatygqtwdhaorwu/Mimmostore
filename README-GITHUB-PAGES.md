# MimmoStore GitHub Pages – original behavior fix

This version keeps the original application JavaScript/gallery behavior and only redirects the category route loader to the bundled static API data.

Phone: 3511779214

Test in Termux:
```bash
python3 -m http.server 8080 --bind 127.0.0.1
```
Open http://127.0.0.1:8080/
